/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time by vite.config.ts (commit SHA on CI, else build timestamp).
declare const __APP_VERSION__: string;
